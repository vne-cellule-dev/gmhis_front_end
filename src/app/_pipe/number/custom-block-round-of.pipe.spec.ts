import { CustomBlockRoundOfPipe } from './custom-block-round-of.pipe';

describe('CustomBlockRoundOfPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomBlockRoundOfPipe();
    expect(pipe).toBeTruthy();
  });
});
